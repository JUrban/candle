# Direct boundary 05: focused action 152 arithmetic compatibility

Status: DEVELOPMENT / NON-RELEASE focused source-load pass. The last accepted
cumulative metric remains 152/152 actions, indices 000--151. Action 152 has
completed from a fresh clean predecessor, but it receives no cumulative action
credit until a regenerated action-zero plan commits its marker.

## Result

The complete action-152 source, `formal_lp/hypermap/arith_link.hl`, loaded from
a fresh immutable post-action-150 checkpoint after authenticated action 151.
All nested sources completed, `Arith_nat.NUM_ADD_HASH_CONV` was bound, the
pending logical-source list was empty, and the observed head-first loader delta
was exactly:

```text
arith_link.hl
arith_nat.hl
arith_cache.hl
arith_num.hl
misc_vars.hl
misc_functions.hl
arith_options.hl
```

The clean resulting state was captured as a focused development checkpoint at:

```text
/project/flyspeck-candle-runs/v99-post-action152-focused-checkpoint-50d54fab2-uncommitted-dev-001
```

The immutable 2,884,607,805-byte image has SHA-256
`303f4e18e601c50f12515cc346a8dfbb279fb72443b079d27cf1d069543e5e12`.
The snapshot log has SHA-256
`a53eda0e4b8a9fc979cb18680d33d0cb5d87cf1feac9f08a34393837d63f2841`,
and the receipt has SHA-256
`613b4fe7794164346e0073432895b28e81d7656c50c184430e02899ff6951b94`.
The receipt explicitly records that action 152 has focused source-load credit
only and no action-protocol, cumulative, S1, S2, S3, or release credit.

## Compatibility batch

`arith_num.hl` contains all 21 top-level polymorphic `Hashtbl.create` bindings
in the selected arithmetic source. Exact type annotations state the types
already forced by their uses without changing allocation or table behavior.
Its 37 ascending loops and five array assignments are currently exercised by
a transient source containing the direct equivalents generated by the pending
central CakeML frontend repair.

`arith_cache.hl` needed a second complete group of six table annotations. Three
successive clean probes then exposed local value-restriction consequences:
the polymorphic `Hashtbl.clear` alias, unsupported integer-only `sprintf`
formatting, and the polymorphic `Hashtbl.length` alias. The accepted exact-span
normalization expands the two aliases at their typed call sites and replaces
all ten format calls with the same labels, integer fields, punctuation, and
newlines using `string_of_int`. Its normalized identity is SHA-256
`9e6a8c01261db246f15798476f8912e3cbfc9e34c1db91814e25308f60a95dbe`
and MD5 `4435dbc49c013c948975b7132906172c`.

Native OCaml oracle and exact normalization tests validate table value shapes,
all cache resets, signed integer formatting, all 19 printed integer fields,
and all six cache-length observations. At the arithmetic-focused pass the
contract contained 79 entries and had SHA-256
`7f726d738426e80816afa20eb1c7a465852dbeb4c513896c353f3dde43a99a1d`.
The subsequent action-153 candidate adds one independent GLPK entry, bringing
the current fixed-point contract to 80 entries and SHA-256
`8e35902c2c43bfa4d4f5a2ec5bbb2e7b0fe8cc84e7eebbb233d6534a56f1475d`;
it does not change any arithmetic source or normalized arithmetic identity.

## Why the first terminal probe was not credited

The first complete source pass, `v98-focused-action152-arith-cache-length-dev-001`,
then called `candle_flyspeck_stratum_commit_action 152`. The checkpoint's
authenticated plan intentionally ends at action 151 and contains exactly 152
identity and ledger-delta entries, so its closed-over `List.nth` rejected index
152. The preserved log has SHA-256
`f9ffe20b5a371d13ef083f4485d3c911cb7d7cb812d93b4d798f1ba6776166a6`.
This is an expected stale-plan boundary, not an arithmetic, loader, or Flyspeck
source failure. The clean snapshot recreation therefore validates the exact
new delta but does not redefine the old commit function or fabricate a marker.

## Central frontend follow-up

On the isolated CakeML branch, commit `8dc5e5d9618d78a4cdc6afb8346f8c583862b485`
lowers OCaml `for` loops and standalone structure expressions, and commit
`73945eca14ee1866bfa6f96de09bf5744dce485d` lowers the one- and two-level
indexed assignments present in the authenticated Flyspeck graph. The complete
OCaml parser-theory suite passed in 13m42s with 3,524,488 KiB maximum RSS. A
full compiler build and compiled-runtime gates are still required before the
transient loop/array rewrites can be removed.

The immediate functional frontier is action 153, `formal_lp/glpk/glpk_link.ml`,
which is being probed from fresh copies of the clean focused checkpoint. After
the frontend runtime and a coherent action-153 batch are ready, the regenerated
plan must replay from action zero and authenticate action 152 before the
cumulative metric advances.
