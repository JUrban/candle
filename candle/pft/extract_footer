#!/usr/bin/env bash
# Generated by Claude Opus 4.6.
set -euo pipefail

file="$1"
out="${1}.footer"

footer_len=$(tail -c 2 "$file" | od -An -tu2 --endian=little | tr -d ' ')
total=$((footer_len + 2))

tail -c "$total" "$file" > "$out"

echo "Extracted $total-byte footer to $out"
